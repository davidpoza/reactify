import { makeStyles } from '@material-ui/core/styles';

export default makeStyles((theme) => ({
  root: {
    maxWidth: 180,
    margin: 12,
  },
  title: {
    fontSize: '1.1rem',
    fontWeight: 'bold',
  },
  author: {
    fontSize: '1rem',
  }

}));
